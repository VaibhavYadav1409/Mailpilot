# Base URL of the backend API (Phases 1-5). Include the /api suffix — the
# WebSocket hook strips it back off automatically for the socket.io handshake.
NEXT_PUBLIC_API_URL="http://localhost:4000/api"
